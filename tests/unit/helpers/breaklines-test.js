import {
  breaklines
} from '../../../helpers/breaklines';
import { module, test } from 'qunit';

module('BreaklinesHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = breaklines(42);
  assert.ok(result);
});
